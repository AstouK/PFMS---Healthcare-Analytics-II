# Hospital LOS Notebook

## 📂 Repository Contents

- `hospital_los.ipynb` – Jupyter Notebook with all of the code.
- `hospital_los.html` – HTML version of Jupyter Notebook
- `requirements.yml` – Conda environment file with all required dependencies.
- `Topic3_healthcare_analytics_dataset.csv` - CSV file containing full data set.

---

## ⚡ Requirements

You need **Conda** installed to set up the environment. All Python dependencies are listed in `requirements.yml`.  

---
## Environment set up

### Create the environment (will use the name specified in requirements.yml)

conda env create -f requirements.yml

### List environments to verify

conda env list

### Activate environment:

conda activate hospital_los
